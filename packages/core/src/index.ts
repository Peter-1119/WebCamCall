/**
 * @scanner/core
 *
 * Headless barcode / QR scanner. 零依賴、不綁框架、不含 UI。
 * 這個檔案只做 re-export，讓 bundler 能 tree-shake。
 *
 * Phase 1 之後會在這裡匯出型別與 createScanner()。
 */
export const VERSION = '0.0.0' as const
